<?xml version="1.0" encoding="utf-8"?>

<paths>

    <!-- The camera app writes here; PhotoStorage then copies into filesDir. -->
    <cache-path
        name="captures"
        path="captures/" />

</paths>